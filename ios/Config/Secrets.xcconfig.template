// Copy to `Secrets.xcconfig` in this folder (gitignored) and set values for local builds / CI.
// In Xcode: Project → StrideCheck target → Build Settings → set "Based on Configuration File"
// to `StrideCheck/BuildConfig.xcconfig` (or add `#include?` as documented in ios/README.md).
//
// CI: generate `Secrets.xcconfig` from protected env vars before `xcodebuild`.

OURA_PERSONAL_ACCESS_TOKEN =
CRIMEOMETER_API_KEY =
WHOOP_CLIENT_ID =
WHOOP_CLIENT_SECRET =
TRAFFIC_OVERLAY_GEOJSON_URL =
STRAVA_CLIENT_ID =
STRAVA_CLIENT_SECRET =
