import SwiftUI
import UIKit

@main
struct StrideCheckApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
                .background(Color(uiColor: .systemGroupedBackground))
        }
    }
}
